# Copie para .env.capacitor e ajuste a URL do player (termina em /player).
# Usado por capacitor.config.ts ao rodar: npm run android:sync
CAPACITOR_SERVER_URL=https://signix-stream.lovable.app/player
