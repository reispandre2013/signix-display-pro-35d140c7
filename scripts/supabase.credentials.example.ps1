# Copie este arquivo para "supabase.credentials.ps1" e preencha os valores.
# Esse arquivo de exemplo pode ficar versionado; o arquivo real com credenciais deve ficar no .gitignore.

$SupabaseProjectRef = "SEU_PROJECT_ID"
$SupabaseAccessToken = "SEU_SUPABASE_ACCESS_TOKEN"

# Se voce informar token acima, pode manter true para evitar login interativo.
$SupabaseSkipLogin = $true
